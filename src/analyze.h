#ifndef _ANALYZE_H
#define _ANALYZE_H

int Pac_AnalyzeGameState(GAME_STATE);
int AnalyzeIter(GAME_STATE, int, FILE *);

// char getNextChar(GAME_STATE, tDir)
// BOOL isValidMove(GAME_STATE, tDir)

#endif	/* _ANALYZE_H */
